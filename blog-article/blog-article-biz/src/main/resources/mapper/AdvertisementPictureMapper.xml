<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.xzf.blog.article.biz.domain.mapper.AdvertisementPictureMapper">
  <resultMap id="BaseResultMap"
             type="com.xzf.blog.article.biz.domain.dataobject.AdvertisementPictureDO">
    <id column="id" jdbcType="BIGINT" property="id" />
    <result column="url" jdbcType="VARCHAR" property="url" />
    <result column="sort_order" jdbcType="INTEGER" property="sortOrder" />
    <result column="create_time" jdbcType="TIMESTAMP" property="createTime" />
    <result column="update_time" jdbcType="TIMESTAMP" property="updateTime" />
  </resultMap>

  <!-- 所有列 -->
  <sql id="Base_Column_List">
    id, url, sort_order, create_time, update_time
  </sql>

  <insert id="insertBatch" parameterType="java.util.List"
          useGeneratedKeys="true" keyProperty="id">
    <!-- 如果没有数据，直接返回 -->
    <if test="list == null or list.size() == 0">
      SELECT 0
    </if>
    <if test="list != null and list.size() > 0">
      INSERT INTO advertisement_picture
      (url, sort_order, create_time, update_time)
      VALUES
      <foreach collection="list" item="item" index="index" separator=",">
        (
        #{item.url,jdbcType=VARCHAR},
        #{item.sortOrder,jdbcType=INTEGER},
        <!-- 如果有时间，使用传入的时间；否则使用数据库当前时间 -->
        <choose>
          <when test="item.createTime != null">
            #{item.createTime,jdbcType=TIMESTAMP}
          </when>
          <otherwise>
            NOW()
          </otherwise>
        </choose>,
        <choose>
          <when test="item.updateTime != null">
            #{item.updateTime,jdbcType=TIMESTAMP}
          </when>
          <otherwise>
            NOW()
          </otherwise>
        </choose>
        )
      </foreach>
    </if>
  </insert>


</mapper>